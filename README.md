# Proyectos en typescript

    # node-ts-app
        app sencinlla para listar y crear books usando typescript, mongodb y express
    
    # tsc-node-crud
        crud de tareas con typescript usando clases, mongodb, y express